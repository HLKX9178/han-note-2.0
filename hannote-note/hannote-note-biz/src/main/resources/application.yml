server:
  port: 8085

spring:
  application:
    name: hannote-note
  profiles:
    active: dev
  threads:
    virtual:
      enabled: true

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
