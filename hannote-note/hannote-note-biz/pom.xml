<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.hanserwei</groupId>
        <artifactId>hannote-note</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>hannote-note-biz</artifactId>
    <packaging>jar</packaging>
    <name>${project.artifactId}</name>
    <description>笔记服务业务层：频道/话题/笔记业务实现与启动模块。作者：Hanserwei</description>

    <dependencies>
        <!-- 本服务对外契约（DTO / HTTP Interface），biz 侧需引用其中的 DTO -->
        <dependency>
            <groupId>com.hanserwei</groupId>
            <artifactId>hannote-note-api</artifactId>
        </dependency>
        <dependency>
            <groupId>com.hanserwei</groupId>
            <artifactId>hannote-common</artifactId>
        </dependency>
        <!-- 业务接口日志组件 -->
        <dependency>
            <groupId>com.hanserwei</groupId>
            <artifactId>hannote-spring-boot-starter-biz-operationlog</artifactId>
        </dependency>
        <!-- 登录用户上下文组件 -->
        <dependency>
            <groupId>com.hanserwei</groupId>
            <artifactId>hannote-spring-boot-starter-biz-context</artifactId>
        </dependency>

        <!-- 服务间调用组件（HTTP Interface + LoadBalancer） -->
        <dependency>
            <groupId>com.hanserwei</groupId>
            <artifactId>hannote-spring-boot-starter-rpc</artifactId>
        </dependency>
        <!-- 分布式 ID 服务契约（生成笔记 ID） -->
        <dependency>
            <groupId>com.hanserwei</groupId>
            <artifactId>hannote-distributed-id-generator-api</artifactId>
        </dependency>
        <!-- KV 键值服务契约（存取笔记正文） -->
        <dependency>
            <groupId>com.hanserwei</groupId>
            <artifactId>hannote-kv-api</artifactId>
        </dependency>
        <!-- 用户服务契约（详情接口查发布者昵称/头像） -->
        <dependency>
            <groupId>com.hanserwei</groupId>
            <artifactId>hannote-user-api</artifactId>
        </dependency>

        <!-- Caffeine 本地缓存（笔记详情 L1 缓存） -->
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>

        <!-- RocketMQ：广播删本地缓存 + 延迟双删 Redis 缓存 -->
        <dependency>
            <groupId>org.apache.rocketmq</groupId>
            <artifactId>rocketmq-spring-boot-starter</artifactId>
        </dependency>

        <!-- RocketMQ 原生客户端（点赞/收藏批量顺序消费） -->
        <dependency>
            <groupId>org.apache.rocketmq</groupId>
            <artifactId>rocketmq-client</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Guava Preconditions 参数校验 -->
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
        </dependency>

        <!-- 字符串工具 -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
        </dependency>

        <!-- 参数校验运行时（@Validated 需要） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- MyBatis-Plus + PostgreSQL -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Spring Data Redis + Lettuce 连接池（笔记详情缓存等） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-pool2</artifactId>
        </dependency>

        <!-- 服务发现 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>

        <!-- 测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>
</project>
